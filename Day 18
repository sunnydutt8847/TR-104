 React State with useState Hook

Today's Focus: State Management, useState Hook
What I Learned: Managing dynamic data in components. Learned:

useState() hook

State vs Props

Updating state correctly

Multiple state variables

State initialization

Functional updates

Practice Example: Counter App
import { useState } from 'react';

function Counter() {
    const [count, setCount] = useState(0);
    
    const increment = () => setCount(count + 1);
    const decrement = () => setCount(count - 1);
    const reset = () => setCount(0);
    
    return (
        <div>
            <h2>Count: {count}</h2>
            <button onClick={increment}>+</button>
            <button onClick={decrement}>-</button>
            <button onClick={reset}>Reset</button>
        </div>
    );
}
Practice Questions:

Create toggle switch for dark/light mode

Build todo app with add/remove functionality

Make character counter for text input


