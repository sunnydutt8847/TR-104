Working with APIs

Today's Focus: REST APIs, HTTP Methods, JSON Data
What I Learned: Communicating with external services. Learned:

REST API principles

HTTP methods: GET, POST, PUT, DELETE

JSON data format

API keys and authentication

Status codes: 200, 201, 400, 404, 500

Practice Example: CRUD Operations
// GET request
async function getPosts() {
    const response = await fetch('https://jsonplaceholder.typicode.com/posts');
    return await response.json();
}

// POST request
async function createPost(title, body) {
    const response = await fetch('https://jsonplaceholder.typicode.com/posts', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({title, body, userId: 1})
    });
    return await response.json();
}
Practice Questions:

Build blog that fetches posts from JSONPlaceholder

Create user management system with mock API

Make currency converter using exchange rate API


