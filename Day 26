 Firebase Authentication

Today's Focus: User Authentication, Authorization
What I Learned: Implementing secure user authentication. Learned:

Email/password authentication

Google sign-in

User state persistence

Protected routes

User profile management

Error handling

Practice Example: Auth Implementation

import { 
    createUserWithEmailAndPassword, 
    signInWithEmailAndPassword,
    signInWithPopup,
    GoogleAuthProvider,
    signOut,
    onAuthStateChanged
} from 'firebase/auth';

// Sign up with email
const signUp = async (email, password) => {
    try {
        const userCredential = await createUserWithEmailAndPassword(auth, email, password);
        return userCredential.user;
    } catch (error) {
        throw error;
    }
};

// Google sign in
const googleSignIn = async () => {
    const provider = new GoogleAuthProvider();
    const result = await signInWithPopup(auth, provider);
    return result.user;
};

// Auth state listener
onAuthStateChanged(auth, (user) => {
    if (user) {
        console.log("User is signed in:", user.email);
    } else {
        console.log("User is signed out");
    }
});

Practice Questions:

Create complete auth system

Build profile page with user data

Make password reset functionality
