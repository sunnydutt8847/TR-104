 Introduction to Node.js

Today's Focus: Server-side JavaScript, Backend Basics
What I Learned: Running JavaScript on server. Learned:

Node.js runtime environment

npm package manager

Creating server with http module

Handling requests and responses

File system operations

Environment variables

Practice Example: Basic Server

const http = require('http');

const server = http.createServer((req, res) => {
    if (req.url === '/') {
        res.writeHead(200, { 'Content-Type': 'text/html' });
        res.end('<h1>Home Page</h1>');
    } else if (req.url === '/about') {
        res.writeHead(200, { 'Content-Type': 'text/html' });
        res.end('<h1>About Page</h1>');
    } else {
        res.writeHead(404, { 'Content-Type': 'text/html' });
        res.end('<h1>Page Not Found</h1>');
    }
});

server.listen(3000, () => {
    console.log('Server running on port 3000');
});

Practice Questions:

Create simple file server

Build API endpoint returning JSON

Make request logger middleware


